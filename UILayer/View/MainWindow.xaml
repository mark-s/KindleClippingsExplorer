<Window x:Class="ClippingsExplorer.UILayer.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        Title="MainWindow" Height="569" Width="1172" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="29*"/>
            <RowDefinition Height="481*"/>
            <RowDefinition Height="28*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>


        <Button Grid.Column="0" Grid.Row="0"
                Command="{Binding ShowFileChoice}"
                >Open Clippings file</Button>

        <ListBox  Grid.Column="0" ItemsSource="{Binding BookTitles}" Grid.Row="1" />



        <ListBox Grid.Column="1" Grid.Row="1" />

        <TextBox Grid.Column="2" Background="Azure" Grid.Row="1" />


    </Grid>
</Window>
